@using Interception.UI.Application.Interceptions.Dtos
@using Interception.UI.Application.Registry.Dtos

<div class="border rounded p-3">
    <div class="form-check form-switch mb-2">
        <input class="form-check-input"
               id="@($"contour-enable-{IdPrefix}")"
               type="checkbox"
               checked="@Enabled"
               @onchange="OnEnabledChanged"
               disabled="@Saving" />
        <label class="form-check-label fw-semibold" for="@($"contour-enable-{IdPrefix}")">
            Контур керування
        </label>
    </div>

    @if (Enabled)
    {
        @if (!string.IsNullOrWhiteSpace(HintText))
        {
            <div class="small text-muted mb-2">@HintText</div>
        }


        @if (!Loading && Options.Count == 0)
        {
            <div class="alert alert-warning py-2 px-2 small mb-2" role="alert">
                Доступних осіб для контуру поки немає. Спочатку оновіть/підтвердьте учасників у реєстрі осіб,
                після цього поверніться та натисніть «Оновити варіанти осіб».
            </div>
        }

        <div class="mb-2">
            <button type="button"
                    class="btn btn-outline-secondary btn-sm"
                    @onclick="RefreshOptions"
                    disabled="@(Loading || Saving)">
                @if (Loading)
                {
                    <span class="spinner-border spinner-border-sm me-1"></span>
                }
                Оновити варіанти осіб
            </button>
        </div>

        <div class="row g-2">
            <div class="col-12">
                <label class="form-label">Керуючий</label>
                <select class="form-select"
                        value="@FromSelection"
                        @onchange="OnFromSelectionChanged"
                        disabled="@(Loading || Saving)">
                    <option value="">— оберіть особу —</option>
                    @foreach (var option in Options)
                    {
                        <option value="@BuildOptionValue(option)">@BuildOptionLabel(option)</option>
                    }
                </select>
            </div>

            <div class="col-12">
                <label class="form-label">Підпорядкований</label>
                <select class="form-select"
                        value="@ToSelection"
                        @onchange="OnToSelectionChanged"
                        disabled="@(Loading || Saving)">
                    <option value="">— оберіть особу —</option>
                    @foreach (var option in Options)
                    {
                        <option value="@BuildOptionValue(option)">@BuildOptionLabel(option)</option>
                    }
                </select>
            </div>

            <div class="col-6">
                <label class="form-label">Тип комунікації</label>
                <select class="form-select" @bind="Form.RelationType" disabled="@Saving">
                    @foreach (var relationType in Enum.GetValues<CommandContourRelationTypeDto>())
                    {
                        <option value="@relationType">@BuildRelationTypeLabel(relationType)</option>
                    }
                </select>
            </div>

            <div class="col-6">
                <label class="form-label">Впевненість</label>
                <select class="form-select" @bind="Form.Confidence" disabled="@Saving">
                    @foreach (var confidence in Enum.GetValues<CommandContourConfidenceDto>())
                    {
                        <option value="@confidence">@BuildConfidenceLabel(confidence)</option>
                    }
                </select>
            </div>

            <div class="col-12">
                <label class="form-label">Коментар</label>
                <textarea class="form-control"
                          rows="3"
                          @bind="Form.Comment"
                          disabled="@Saving"></textarea>
            </div>
        </div>
    }
</div>
